# 2026-04-24 工作记录

## 法规周报完整版生成 + Skill修复

### 本次执行

- 任务：法规周报（2026-04-17~2026-04-25）完整版不过滤去重
- 总条目：41条（CN 18 + EU 23）
- 新发现的EU重要条目（之前被dedup拦截）：EMDN FAQ、EU MDR改革提案(COM/2025/800, 170页)、MDCG 2021-24 rev.1、边境分类手册2026更新、成员国临床试验要求更新、制造商不良事件XSD/SB11154、MDCG 2020-16 rev.3(IVDR分类)
- IMA上传：✅ note_id: 7453261212699682

### 本次修复的问题

#### 1. 去重逻辑bug（`deduplicator.py`）

**问题**：原逻辑用 `URL OR title` 任一匹配即去重，EU来源做 in-place 更新（URL不变但标题/日期变）时被误拦。

**修复**：
- 旧缓存：`self._seen_urls` + `self._seen_titles`（独立集合）
- 新缓存：`self._seen_keys` 统一集合，格式 `"source#url"` 和 `"source#title_normalized"`
- `is_seen()` 改为：必须同时传 `source`，复合键匹配才算重复
- `filter_new()` 传入 `source` 参数
- 容错：`_load()` 和 `add()` 对 `None` source 兼容处理

#### 2. EU网站大规模改版（`sources.json`）

- `ec_guidance`（MDCG指南）：原URL已404，更新为 `/medical-devices-sector-new-regulation/` 路径
- EU网站大量原URL 404，需后续逐一验证新URL

#### 3. 新增4个EU来源（`sources.json`）

- `ec_emdn`：EMDN欧洲医疗器械命名法官方页（🆕 web_fetch）
- `ec_mdr_revision`：EU MDR/IVDR Targeted Revision提案页（🆕 web_fetch）
- `ec_borderline`：边境分类手册（含数字健康产品边界判定）（🆕 web_fetch）
- `ec_clinical_national`：成员国临床试验国家要求（🆕 web_fetch）
- `ec_mir_xsd`：制造商不良事件报告XSD/SB 11154（🆕 web_fetch）

`web_fetch_urls` 新增 `new_eu_2026` 列表，包含以上来源的可抓取URL。

#### 4. SKILL.md 更新

- 去重策略说明更新为复合键逻辑
- 新增来源列表记录

### 来源配置补全（上午追加）

**用户反馈**：FDA新闻也有更新，要求检查各地区来源是否遗漏。

**检查结果**：
| 地区 | 状态 | 说明 |
|------|------|------|
| FDA CDRH新闻 | ✅ 已配置 | 用户提供的链接已在配置中 |
| FDA Safety Communications | 🆕 新增 | https://www.fda.gov/medical-devices/medical-device-safety/safety-communications |
| EU | ✅ 已配置 | 上次已补全 |
| NMPA | ✅ 已配置 | — |
| Health Canada Announcements | 🆕 新增 | https://www.canada.ca/en/health-canada/services/drugs-health-products/medical-devices/activities/announcements.html |

**本次更新**：
- `sources.json` 新增 `fda_safety_comm`（FDA安全通讯）和 `ca_announcements`（加麻大公告）
- `web_fetch_urls` 新增 `new_us_ca_2026` 列表
- SKILL.md 更新日志已更新

### 待完成项（TODO）

- [ ] 验证新增EU来源URL是否可正常抓取（web_fetch）
- [ ] `ec_guidance` 新URL待验证
- [ ] EU网站改版后其他旧URL也需要逐一验证
- [ ] 考虑去重时增加日期校验（同一来源+同标题但发布周期不同应视为新条目）
- [ ] 验证 `fda_safety_comm` 和 `ca_announcements` 的 web_fetch 可用性

### 根因分析：FDA CDRH新闻为什么没抓到

**问题**：`parsers.py::fetch_source()` 对 `type="web_fetch"` 来源直接返回 `[]`，静默跳过。AI的 web_fetch 工具从未收到这些URL。

**证据**：run_tracker.py 没有调用 web_fetch_scraper.py；web_fetch_urls 只存在于 sources.json 配置中，从未进入执行流。

**修复**：
- 新增 `fetch_all_sources_with_webfetch_tracking()` 函数
- 对 web_fetch 来源：收集到 `web_fetch_sources` 列表，生成 `web_fetch_pending_{日期}.json`
- 脚本打印 `[⚠️ 需AI补抓]` 提示，不再静默跳过
- `run_tracker.py` 支持 `--web-fetch-items` 注入 web_fetch 结果

### 本次法规周报生成（下午）

**策略**：web_fetch对FDA/CMDE/NMPA均超时 → 智能降级，立即切web_search补充

**最终数据来源**：
- web_search（主要）：FDA CDRH news、EU MDR/MDCG、CN NMPA/CMDE、CA Health Canada、IMDRF
- web_search补充覆盖：CDRH New 4/21、4/23、FDA READI-Home、MDCG 2021-24 Rev.1、EU MDR修订提案、CMDE 5项指导原则、加拿大REP、IMDRF信赖Playbook等

**报告文件**：`scripts/reports/MDR_Weekly_Report_2026-W17.md`

**经验**：
- FDA.gov/CMDE/NMPA主页web_fetch超时是长期问题，应默认web_search为主渠道
- 遇到web_fetch失败立即切搜索，不要重试
- 各地区关键媒体（Mintz、RAPS、MedTech Dive、海河观瞻等）是很好的摘要来源

### IMA上传修复（重要踩坑）

**问题**：之前用的 KB ID `2vKqXH6BRpnhgQ2P` 是占位符/错误ID，`add_knowledge` 一直返回 invalid knowledge_base_id。

**正确 ID**：
- 「MD法规更新汇总」→ `ihJNh4Nl87Wqv-XLfXyOJSW1NMOBnVnk-HBnlcYgL4o=`（通过 `get_addable_knowledge_base_list` 获取）
- 「Hannah的知识库」→ `i6YyWgwwoeNGXqrtspeovyL6QKXfhJWHaAqySYqbbY4=`
- 「脑机接口」→ `yQmDTuK0h_6ceac7Minb3Vgj5Zj6Tbr3ICNwGtezZTM=`
- 「FDA认证汇总知识库」→ `M_daNwQAaqov4X8CLnjCOguVG2lqiWLO1vbxpAW3K2c=`
- 「其他地区」→ `7fqkqco92Xs-7wGCYYKaHSJkxEUUOaw9gdxAkAVMQx0=`
- 「产品学习资料」→ `FesysgjYSFPjWff-d6wAyG3m3CEB1Qh4QS0d2dlryOw=`
- 「CE与FDA法规解读」→ `sulzXmP_VIFmLOvFjr_wTZLdJPUEpfpzAKV4wdmHOU0=`

**正确上传流程**（将报告内容作为笔记添加到知识库）：
1. 笔记模块创建笔记：`POST /openapi/note/v1/import_doc` → 返回 `data.note_id`
   - 注意：笔记模块返回格式用 `code=0`（不是 retcode=0），`note_id` 在 `data.note_id`
2. 知识库模块关联笔记：`POST /openapi/wiki/v1/add_knowledge`
   - 参数：`media_type=11`, `note_info.content_id=<note_id>`, `title`, `knowledge_base_id`
   - 返回格式：用 `code=0`（不是 retcode=0），`media_id` 在 `data.media_id`
